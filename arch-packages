#!/bin/bash

if [[ $EUID == 0 ]]; then
  PACMANCMD=( pacman )
else
  PACMANCMD=( sudo pacman )
fi

# Install packages from official repositories

BASE="base base-devel"
DEV="cmake docker docker-compose git git-lfs go jdk-openjdk mono neovim nodejs powerline python python-pip python2 ruby rust tmux vagrant virtualbox"
GNOME="baobab evince file-roller gdm gnome-backgrounds gnome-boxes gnome-calculator gnome-characters gnome-clocks gnome-color-manager gnome-control-center gnome-disk-utility gnome-font-viewer gnome-getting-started-docs gnome-keyring gnome-logs gnome-menus gnome-remote-desktop gnome-screenshot gnome-session gnome-settings-daemon gnome-shell gnome-shell-extensions gnome-system-monitor gnome-terminal gnome-themes-extra gnome-user-docs gvfs gvfs-afc gvfs-goa gvfs-google gvfs-gphoto2 gvfs-mtp gvfs-nfs gvfs-smb mousetweaks mutter nautilus networkmanager rygel simple-scan sushi tracker tracker-miners xdg-user-dirs-gtk yelp"
GNOMEEXTRA="dconf-editor gnome-nettool gnome-tweaks gnome-usage libsecret seahorse"
DESKTOPAPPS="keepass"
THEMING="adapta-gtk-theme gtk-engine-murrine noto-fonts noto-fonts-cjk noto-fonts-emoji ttf-roboto"
OTHERS="alsa-utils fcitx fcitx-googlepinyin fcitx-mozc ntp xclip xdotool xsel"
export ALLPACKAGES="$BASE $DEV $GNOME $GNOMEEXTRA $DESKTOPAPPS $THEMING $OTHERS"

"${PACMANCMD[@]}" -Syuq
"${PACMANCMD[@]}" -Sq --needed $ALLPACKAGES || echo "This script must be run as either root or a sudoer to install pacman packages!"

# Install packages from AUR
AURPACKAGES="anaconda android-studio debtap discord google-chrome powerline-fonts-git spotify synology-drive unityhub"
if [ -x /usr/bin/yay ]; then
  echo "Yay already installed!"
else
  git clone https://aur.archlinux.org/yay.git
  cd yay
  makepkg -si
  "${PACMANCMD[@]}" -U *.pkg.xz || echo "This script must be run as either root or a sudoer to install pacman packages!"
  cd ..
  rm -rf yay
fi
yay -Sq --needed $AURPACKAGES

if ! pacman -Qqs "chat"; then
  chatname='Chat_1.0.1-40_amd64.deb'
  chaturl="https://global.download.synology.com/download/Tools/ChatClient/1.0.1-40/Ubuntu/x86_64/Chat_1.0.1-40_amd64.deb?model=DS1517&bays=5&dsm_version=6.2.2&build_number=24922"
  curl "$chaturl" > "$chatname"
  debtap "$chatname"
fi

# Activate software
sudo systemctl enable docker.service
sudo systemctl start docker.service
