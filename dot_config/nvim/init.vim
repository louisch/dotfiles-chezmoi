" Plugins must be loaded before configuring anything else
runtime setup.vim
runtime appearance.vim
" Set up leader key in behavior before configuring plugins
" (which use leader bindings)
runtime behavior.vim
runtime plugins.vim
